from django.urls import path
from rest_framework.routers import DefaultRouter

# Import views (uncomment when views are created)
# from . import views

# Router for ViewSets
# router = DefaultRouter()
# router.register(r'resource', views.notificationsViewSet, basename='notifications')

urlpatterns = [
    # Add your URL patterns here
    # Example: path('', views.list_view, name='list'),
    # Example: path('<int:pk>/', views.detail_view, name='detail'),
]

# Add router URLs (uncomment when router is active)
# urlpatterns += router.urls
